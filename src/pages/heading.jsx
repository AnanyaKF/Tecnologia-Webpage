import React from 'react';

const heading = () => {
  const headingStyle = {
    textAlign: 'center',
  };

  return (
    <div>
      <h1 style={headingStyle}><b>We manage your IT, so you can manage your business.</b></h1>
    </div>
  );
};

export default heading;